server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/user/**, /api/auth/**
        - id: news-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/news/**
        - id: learning-service
          uri: http://localhost:8083
          predicates:
            - Path=/api/learning/**

jwt:
  secret: ${JWT_SECRET}