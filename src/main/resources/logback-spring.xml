<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ✅ Spring Boot 기본 콘솔 스타일 유지 -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- ✅ GatewayCustomLogger 전용 로그 파일 appender (20분 단위 분할) -->
    <appender name="GATEWAYLOGGER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>../logs/service/gateway-service/gateway.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- ✅ 분 단위 롤링 (20분 단위처럼 활용 가능) -->
            <fileNamePattern>../logs/service/gateway-service/gateway.%d{yyyy-MM-dd_HH-mm}.log</fileNamePattern>
            <maxHistory>90</maxHistory> <!-- 약 하루 3개 × 24시간 = 72개, 여유분 포함 -->
        </rollingPolicy>
        <encoder>
            <!-- 예외 포함 + 운영용 상세 포맷 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n%ex</pattern>
        </encoder>
    </appender>

    <!-- ✅ 루트 logger: 콘솔 출력만 유지 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- ✅ GatewayCustomLogger 전용 파일로만 출력 -->
    <logger name="edu.goorm.gatewayservice.global.logger.GatewayCustomLogger"
      level="INFO"
      additivity="false">
        <appender-ref ref="GATEWAYLOGGER"/>
    </logger>

</configuration>
